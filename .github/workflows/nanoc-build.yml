name: Build Site with Nanoc

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install gems
      run: bundle install
    - name: Build the site
      run: nanoc compile
